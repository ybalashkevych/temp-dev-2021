name: Validation

on:
  pull_request:
    types: [opened, synchronize, reopened]

env:
  SWIFT_ENABLE_EXPERIMENTAL_FEATURES: "0"
  DERIVED_DATA_PATH: DerivedData
  COVERAGE_THRESHOLD: "20.0"

permissions:
  contents: read
  pull-requests: write
  issues: write

jobs:
  lint-and-format:
    name: Code Quality Checks
    runs-on: macos-26
    timeout-minutes: 2
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install SwiftLint
        run: brew install swiftlint

      - name: Run SwiftLint
        id: swiftlint
        run: |
          echo "Running SwiftLint with config..."
          swiftlint lint > swiftlint-output.txt 2>&1 || echo "SWIFTLINT_FAILED=true" >> $GITHUB_ENV
          cat swiftlint-output.txt
        continue-on-error: true

      - name: Run swift-format
        id: swiftformat
        run: |
          echo "Running swift-format with config..."
          # Exclude Generated directory from formatting
          find LiveAssistant LiveAssistantTests -name "*.swift" ! -path "*/Generated/*" -print0 | \
            xargs -0 xcrun swift-format lint --configuration .swift-format > swiftformat-output.txt 2>&1 || echo "SWIFTFORMAT_FAILED=true" >> $GITHUB_ENV
          cat swiftformat-output.txt
        continue-on-error: true

      - name: Upload lint results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lint-results
          path: |
            swiftlint-output.txt
            swiftformat-output.txt
          retention-days: 1

      - name: Check lint results
        run: |
          if [ "$SWIFTLINT_FAILED" == "true" ] || [ "$SWIFTFORMAT_FAILED" == "true" ]; then
            echo "❌ Code quality checks failed"
            exit 1
          fi
          echo "✅ All code quality checks passed"

  build-test-and-coverage:
    name: Build, Test & Coverage
    runs-on: macos-26
    needs: lint-and-format
    timeout-minutes: 10

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.9'

      - name: Show Xcode version
        run: xcodebuild -version

      - name: Cache Build Artifacts
        uses: actions/cache@v4
        with:
          path: |
            .build
            DerivedData
            ~/Library/Caches/org.swift.swiftpm
          key: ${{ runner.os }}-xcode-${{ hashFiles('**/Package.resolved') }}
          restore-keys: |
            ${{ runner.os }}-xcode-

      - name: Clean stale build artifacts
        run: |
          echo "Cleaning SwiftLint plugin artifacts only..."
          rm -rf DerivedData/*/Intermediates.noindex/PluginCache 2>/dev/null || true
          rm -rf DerivedData/*/Intermediates.noindex/com.apple.dt.Xcode.plugin-cache 2>/dev/null || true
          echo "SwiftLint artifacts cleaned; preserving build cache"
            
      - name: Report cache status
        run: |
          echo "=== SPM Cache Status ==="
          if [ -d "$HOME/Library/Caches/org.swift.swiftpm" ]; then
            du -sh "$HOME/Library/Caches/org.swift.swiftpm" 2>/dev/null || echo "SPM cache exists"
          else
            echo "SPM cache is empty (first run)"
          fi
          if [ -d "DerivedData" ]; then
            du -sh DerivedData 2>/dev/null || echo "DerivedData exists"
          fi
          
      - name: Disable SwiftLint plugin for CI
        run: bash scripts/ci/disable-swiftlint-plugin.sh
          
      - name: Download Lint Results
        uses: actions/download-artifact@v4
        with:
          name: lint-results

      - name: Build and run tests with coverage
        id: test
        run: |
          echo "=== Build Started at $(date) ==="
          xcodebuild test \
            -scheme LiveAssistant \
            -destination 'platform=macOS,arch=arm64' \
            -testPlan LiveAssistant \
            -enableCodeCoverage YES \
            -configuration Debug \
            -derivedDataPath "$DERIVED_DATA_PATH" \
            -skipPackagePluginValidation \
            -skipMacroValidation \
            CODE_SIGN_IDENTITY="-" \
            CODE_SIGNING_ALLOWED=YES \
            -resultBundlePath TestResults.xcresult 2>&1 | tee test-output.txt || echo "TESTS_FAILED=true" >> $GITHUB_ENV
          echo "=== Build Completed at $(date) ==="
        continue-on-error: true

      - name: Generate coverage report
        if: always()
        run: |
          echo "📊 Checking xcresult bundle..."
          ls -lh TestResults.xcresult/ || echo "❌ xcresult not found"
          
          # Check if xcresult exists
          if [ ! -d "TestResults.xcresult" ]; then
            echo "0.00" > coverage_result.txt
            echo "FAILED" > coverage_status.txt
            echo "TESTS_FAILED=true" >> $GITHUB_ENV
            echo "⚠️ TestResults.xcresult not found - tests may have failed before completion"
            exit 0
          fi
          
          echo "📊 Generating coverage JSON..."
          if ! xcrun xccov view --report --json TestResults.xcresult > coverage.json 2>&1; then
            echo "0.00" > coverage_result.txt
            echo "FAILED" > coverage_status.txt
            echo "TESTS_FAILED=true" >> $GITHUB_ENV
            echo "⚠️ Failed to generate coverage report"
            exit 0
          fi
          
          echo "📊 Coverage JSON size:"
          ls -lh coverage.json
          
          # Calculate coverage using Python script
          python3 scripts/ci/calculate_coverage.py $COVERAGE_THRESHOLD || true

      - name: Extract test failures
        if: always()
        run: |
          echo "TEST_ERRORS<<EOF" >> $GITHUB_ENV
          if [ "$TESTS_FAILED" == "true" ]; then
            grep -E "error:|failed:|Test Case.*failed" test-output.txt | head -20 || echo "Tests failed but no specific errors captured"
          fi
          echo "EOF" >> $GITHUB_ENV

      - name: Generate PR comment
        if: always()
        run: python3 scripts/ci/report_pr_results.py

      - name: Comment on PR
        uses: actions/github-script@v7
        if: always() && hashFiles('pr-comment.md') != ''
        with:
          script: |
            const fs = require('fs');
            
            // Check if comment file exists
            if (!fs.existsSync('pr-comment.md')) {
              console.log('⚠️ pr-comment.md not found, skipping PR comment');
              return;
            }
            
            const comment = fs.readFileSync('pr-comment.md', 'utf8');
            
            // Find existing bot comment
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });
            
            const botComment = comments.find(c => 
              c.user.type === 'Bot' && c.body.includes('PR Validation Results')
            );
            
            if (botComment) {
              console.log(`Updating existing comment ${botComment.id}`);
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body: comment
              });
            } else {
              console.log('Creating new comment');
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: comment
              });
            }

      - name: Upload test results and coverage
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-and-coverage-results
          path: |
            TestResults.xcresult
            coverage.json
            coverage_result.txt
            test-output.txt
          retention-days: 7

      - name: Check final status
        run: |
          COVERAGE_STATUS=$(cat coverage_status.txt 2>/dev/null || echo "UNKNOWN")
          
          if [ "$TESTS_FAILED" == "true" ]; then
            echo "❌ Tests failed"
            exit 1
          fi
          
          if [ "$COVERAGE_STATUS" == "FAILED" ]; then
            COVERAGE=$(cat coverage_result.txt)
            echo "❌ Coverage $COVERAGE% is below threshold $COVERAGE_THRESHOLD%"
            exit 1
          fi
          
          echo "✅ All checks passed"
